services:
  image-receiver:
    build:
      context: .
      dockerfile: services/image-receiver/Dockerfile
  image-processor:
    build:
      context: .
      dockerfile: services/image-processor/Dockerfile

networks:
  default:
    name: shiki-network
