services:
  image-receiver:
    build:
      context: .
      dockerfile: services/image-receiver/Dockerfile
    tmpfs:
      - /data:size=256M,mode=0755
    ports:
      - 3001:3000
  image-processor:
    build:
      context: .
      dockerfile: services/image-processor/Dockerfile

networks:
  default:
    name: shiki-network
